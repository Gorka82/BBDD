Esta es una base de datos creada para un evento en estella. El archivo principal es LUNAS, y cuando se solicita se añade la BBDD. Esto se diseño asi para permitir concurrencia de varios usuarios.
BBDD esta poblada por datos falsos, que solo sirven para testear la misma.
